#import { Object } into Namespace from "uri"

type Module {
  # some comment
  func1(
    arg: Bytes!
  ): UInt32!

  func2(
    custom: Custom!
  ): Int32
}

type Custom {
  prop1: String!
  nested: Nested
}

type Nested {
  prop: UInt8
  importedObj: Namespace_Object!
}
